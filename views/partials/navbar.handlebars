<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav">
      <li class="nav-item {{#if home}}active{{/if}}">
        <a class="nav-link {{#if home}}active{{/if}}" href="/">Home {{#if home}}<span class="sr-only">(current)</span>{{/if}}</a>
      </li>
      <li class="nav-item {{#if saved}}active{{/if}}">
        <a class="nav-link {{#if saved}}active{{/if}}" href="/saved">Saved {{#if saved}}<span class="sr-only">(current)</span>{{/if}}</a>
      </li>
      <li class="nav-item">
        <button class="nav-link scrape">Scrape</button>
      </li>
    </ul>
  </div>
</nav>